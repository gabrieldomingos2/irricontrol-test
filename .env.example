# .env.example - Arquivo de exemplo para configurações de ambiente
# Copie este arquivo para .env e preencha com seus valores reais.

# --- Configurações da Aplicação (opcional, se os padrões em config.py forem suficientes) ---
# APP_NAME="Irricontrol Signal Simulator API v1.0"
# APP_VERSION="1.0.1"
# API_V1_STR="/api/v1_dev"

# --- Configurações de CORS ---
ALLOWED_ORIGINS_CSV="http://localhost:3000,http://localhost:8080,http://127.0.0.1:5173,null"

# URL específica da sua aplicação no Netlify (ou outra plataforma de frontend)
NETLIFY_APP_URL="https://sua-app.netlify.app" # Substitua pela URL real do seu frontend em produção

# --- Configurações da API CloudRF ---
# !!! ESTA É A VARIÁVEL MAIS IMPORTANTE E DEVE SER MANTIDA EM SEGREDO !!!
# Substitua pelo seu API Key real da CloudRF no seu arquivo .env local.
CLOUDRF_API_KEY="" # NUNCA COLOQUE A CHAVE REAL AQUI NO .env.example

# URL da API CloudRF (opcional, se diferente do padrão em config.py)
# CLOUDRF_API_URL="https://api.cloudrf.com/area"

# --- Configurações Gerais (opcional, se os padrões em config.py forem suficientes) ---
# HTTP_TIMEOUT="90.0"

# --- Configurações do Servidor (opcional) ---
# SERVER_HOST="0.0.0.0"
# SERVER_PORT="8001"